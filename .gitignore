# MCP 設定（含 API keys，不可提交）
.mcp.json
!.mcp.json.*.example

# Claude Code 個人設定
.claude/settings.local.json

# DevTeam transient session state（hook 高頻變動 / session lock，不入 git）
.claude/context/devteam/consistency-drift.md
.claude/scheduled_tasks.lock

# TaskMaster session-level 暫存檔（hook 高頻變動，不該入 git）
# 保留 timelog.jsonl（append-only 歷史紀錄）
.claude/taskmaster-data/.session-snapshot
.claude/taskmaster-data/.session-start
.claude/taskmaster-data/.current-task

# 獨立子專案（Launch Atlas 圖鑑，自有 repo / 575M 含 node_modules，不入此 repo）
product_to_launch/

# Node
node_modules/

# Python
__pycache__/
*.py[cod]
venv/
.env

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# 暫存
tmp/
*.tmp
*.bak
