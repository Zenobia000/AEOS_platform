[tool.poetry]
name = "aeos-mvg"
version = "0.1.0"
description = "AEOS MVG W1 slice — offline B1 validation (messy knowledge -> usable CS draft)"
authors = ["AEOS"]
package-mode = true
packages = [{ include = "aeos_mvg" }]

[tool.poetry.dependencies]
python = "^3.11"
anthropic = "*"
python-dotenv = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
