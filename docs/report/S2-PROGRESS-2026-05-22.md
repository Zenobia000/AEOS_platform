---
id: REPORT-S2-PROGRESS
title: S2 進度報告 (2026-05-22 snapshot)
date: 2026-05-22
owner: CTO
type: one-shot progress report
period: 2026-05-17 ~ 2026-05-22
related: [LAUNCH-DASHBOARD, DEV-PLAN-PHASE1-2026-05, S1-PROGRESS-2026-05-17, ADR-0011, ADR-0012, db-schema, MC-001, MC-008, MC-009, MC-010]
---

# S2 進度報告

> S2 (KB & KC) sprint 在無 pilot 客戶下提前推進「pilot-independent
> 系統開發」部分。本期重點：DB Foundation + Tier 1 資料層（9/25 表完成）。
>
> 同步更新對象：`LAUNCH-DASHBOARD.md`、`DEV-PLAN-PHASE1-2026-05.md` §4.2 S2 任務、
> `SEC-001` §6.1 #4 RLS、`KICKOFF-CHECKLIST.md` §D.3 必讀。

---

## 1. 摘要

| 維度 | 期初（05-17）| 期末（05-22）| 變動 |
|---|---|---|---|
| DB 表完成 | 0 / 25 | **9 / 25** | +9 (36%) |
| Test 數量 | 2 | **40** | +38 |
| Test coverage | 100%（極小範圍）| **99.68%** | 大範圍仍維持 |
| 程式碼行數 (app + tests + alembic) | 97 | **2943** | +2846 |
| Migration 數量 | 0 | **3** | +3 |
| SEC §6.1 Go/No-Go | 1 / 13 ✅ | **2 / 13 ✅**（+2 部分） | +1 |
| 開啟 branch (open) | 1 (feat/s1-scaffold-ci) | **3** | +2 |
| Commit (vs main) 累積 | 13 | **20+** | +7 |
| ADR 累計 | 12 | **12** | 0 |
| 評估報告 | 2 (pi + nanobot) | **2** | 0 |
| 進度/blocker 報告 | 2 (blockers + S1 progress) | **3** | +1 (本檔) |

**一句話結論**：在 pilot 客戶仍未簽下情況下，**完成 Tier 0 基礎設施 + Tier 1 全部資料層**（9 張表 + RLS + pgvector + 月分區 + audit append-only trigger）。S4 LINE+Draft Mode、S3 Skill Registry、Employee Runtime 一旦動工，儲存層可立即接上。

---

## 2. 已完成（3 個 feat branch 全部 push 到 origin）

### 2.1 ✅ `feat/s2-db-foundation`（3 commits + 1 force-push 對齊契約）

DB Foundation：alembic + Tenant + ApiKey + AuditLog + RLS + audit append-only trigger。

**契約對齊事件**：原 commits 違反 `db-schema.md` §1 命名 convention（用 plural / VARCHAR / native enum），透過 git soft-reset + 重寫 + force-push 修正為「單數 / TEXT / TEXT+CHECK」契約相容版本。

對應規格：
- ADR-0011（Python + FastAPI + SQLAlchemy）
- ADR-0007（共享 PG + RLS）
- ADR-0006（API Key 部分）
- MC-001（audit append-only）
- MC-004（Tenant 4 態狀態機）
- db-schema.md §4.1（tenant）+ §1 命名 convention

### 2.2 ✅ `feat/s2-knowledge-cards`（1 commit）

KnowledgeCard + IngestionJob：5 card_type / 3 status / pgvector(1024) ivfflat + cosine + GIN tags + RLS。

對應規格：
- MC-008 Knowledge RAG
- db-schema.md §4.2 + §4.3
- S2 任務塊「pgvector embedding 寫入；ivfflat index 建立」✅

### 2.3 ✅ `feat/s2-conversation-engine`（1 commit）

Employee + Conversation + Message（partition by month, 8 個分區 2026-05~12）+ ConversationHandoff。

對應規格：
- MC-009 Employee Runtime（Frozen Runtime snapshot via runtime_snapshot JSONB）
- MC-010 Conversation Engine（6 態 status / 4 outcome / handoff reasons）
- ADR-0005 PII pseudonymize（end_user_pseudo_id / channel_user_id）
- ADR-0010 L2.5 session summary（summary 欄位預留）
- db-schema.md §4.1 + §4.4 + §4.5 + §4.6

---

## 3. 驗證 snapshot

| 檢查 | 結果 |
|---|---|
| `pytest` | ✅ 40 passed |
| Coverage | ✅ 99.68% / 80% gate |
| `ruff check` | ✅ All checks passed |
| `ruff format --check` | ✅ 30 files formatted |
| `mypy strict` | ✅ no issues in 29 source files |
| `alembic upgrade head` | ✅ 3 migrations 到 `7b2dc371f434 (head)` |
| 本機 PG relations | ✅ 18（9 base + 8 message partition + 1 alembic_version）|
| pgvector ivfflat cosine query | ✅ smoke `ORDER BY embedding <=> :qv` 跑通 |
| Message monthly partition routing | ✅ 寫入路由到 `message_2026_05` 子分區 |
| `git push -u origin <branch>` × 3 | ✅ 全部已上 GitHub |

---

## 4. Engineering Health 變動

對應 `LAUNCH-DASHBOARD.md` §「Engineering Health」表格：

### 4.1 品質與安全

| 指標 | 期初 | 期末 |
|---|---|---|
| Test coverage（main）| 100%（範圍極小）| **99.68%**（範圍大幅擴）|
| Test 數量 | 2 | **40** |
| Flaky tests | 0 | 0 |
| Open critical CVE | 0 | 0 |
| **SEC-001 §6.1 Go/No-Go** | **1 / 13 ✅** | **2 / 13 ✅**（+2 部分）|

SEC §6.1 進度：
- #4 RLS 啟用：**部分** ✅ — 9 表全部 ENABLE RLS（tenant / api_key / audit_log / knowledge_card / ingestion_job / employee / conversation / message / conversation_handoff），但 cross-tenant query 完整 e2e 測試需 non-owner role 才能反映 prod 真實情況，標 partial。

### 4.2 交付節奏

| 指標 | 期初 | 期末 |
|---|---|---|
| 程式碼行數 (app + tests + alembic) | 97 | **2943** |
| Commit 數 (vs main) | 13 | **20+** |
| 開啟分支數 | 1 | 3 |
| DB 表完成 (Phase 1 共 25 張) | 0 | **9** (36%) |

其他維度（可用性、latency、cost、oncall）仍 n/a — 因為 prod 環境尚未存在。

---

## 5. Tier 完成度地圖

```
Tier 0  基礎設施 ✅✅✅ (3 / 3)
  ├── DB session + RLS context middleware ✅
  ├── Audit Service core (MC-001) ✅
  └── Docker Compose for local dev ✅

Tier 1  Auth + Data Models ✅✅✅✅ (4 / 4)
  ├── Tenant + ApiKey models (MC-004) ✅
  ├── AuditLog (MC-001) ✅
  ├── KnowledgeCard + IngestionJob + pgvector (MC-008) ✅
  └── Conversation + Message (partition) + Handoff + Employee (MC-009, MC-010) ✅

Tier 2  LLM + Tool 🔜 (0 / 3)
  ├── LLMClient 薄層 (ADR-0001 Anthropic) 🔜
  ├── Tool Registry + Policy YAML parser (MC-006) 🔜
  └── Skill repo 結構 + loader (MC-005) 🔜

Tier 3  Runtime ⏳ (0 / 3)
  ├── EmployeeRuntime (MC-009) ← 借鑑 nanobot
  ├── Conversation Engine 狀態機 (MC-010)
  └── Hook 系統 (Audit + Policy + Quota)

Tier 4  Channel + UI ⏳ (0 / 3)
  ├── LINE webhook (HMAC + dedup + 1s ACK)
  ├── KC CRUD UI
  └── Expert review UI

Tier 5  Hardening ⏳ (0 / 2)
  ├── aeos_app non-owner role + 完整 RLS e2e 測試
  └── KB ingest pipeline worker
```

---

## 6. 下一步建議

### 6.1 接下來（pilot-independent，可立即動）

**Tier 2 — LLM + Tool**（預估 2-3 天）：
1. `app/llm/client.py` — `LLMClient` interface + `AnthropicClient` 實作（ADR-0001 薄層）
2. `app/db/models/{skill,skill_version,skill_binding}.py` + migration（MC-005 Skill Registry）
3. `app/db/models/{tool,tool_policy,tool_invocation}.py` + migration（MC-006 Tool Registry）
4. `skills/customer-service/faq-respond/v1.0.0/` git repo 結構（git monorepo + YAML manifest）

### 6.2 ADR-0012 後續更新

ADR-0012 §Operational Updates 列了 2 件當初漏做，本期合併補上：
- ✅ `DEV-PLAN-PHASE1-2026-05.md` §2 Runtime 行：本期同步補上「ADR-0012 借鑑設計 + 自寫精簡版」
- ✅ `KICKOFF-CHECKLIST.md` §D.3 工程必讀：本期同步補上 ADR-0012

### 6.3 仍受阻（critical path）

- **S2~S8 整段產品 code 上線」**：仍 hard-gated by pilot 客戶簽約
- **S1-4 OBS infra**：仍卡 Hetzner 帳號
- **S1-7 Oncall**：仍卡 Slack + PagerDuty

---

## 7. 與計畫的對齊度

對應 `DEV-PLAN-PHASE1-2026-05.md` §4.2 S2 任務塊：

| S2 任務塊 | 狀態 |
|---|---|
| 專案骨架（FastAPI + Alembic + Docker Compose + CI pipeline）| ✅ |
| Auth 基礎（Tenant + API Key + RLS context middleware）| ✅ |
| DB migration（25 張表）| 🟡 9/25（36%，依 sprint 漸進加）|
| KB ingest pipeline (Worker) | 🚫 待 pilot 客戶（需真實 KB）|
| pgvector embedding（schema + ivfflat index）| ✅ schema 層完成；實際 embedding 寫入待 Worker |
| KC CRUD UI（Web SPA）| 🚫 待 S2 前端 sprint |
| Audit Service 接線 | ✅ append-only DB trigger + AuditService.emit |

**Exit Gate（AC-001 三條全綠）狀態**：
- ❓ PDF ingest <3min/100頁 — 待 Worker
- ❓ KC approve 走 audit — 已具備機制，待業務 endpoint 接線
- ❓ archive 不被檢索 — 已具備（status check + RLS 配合）；待測試覆蓋

---

## 變更紀錄

| 日期 | 變更 | Owner |
|---|---|---|
| 2026-05-22 | 初版（S2 進度 Tier 0 + Tier 1 完成 9/25 表）| CTO |
